var arr = [2, 56, 78, 34, 65];
var new_arr = arr.map(function(num) {
return num / 2;
});
print(new_arr);

//output : there will be error as there is no any print method in the Javascript.

//and if console.log is used in place of print the output will be an array containing:
// [1,28,39,17,32.5]

